/**
 * Default model name used as fallback when no models are detected.
 * The actual model is configured via the server's config.toml or
 * the onboarding/settings UI. This is only the client-side fallback.
 */
export const DEFAULT_MODEL = "llama-3.2-3b"
